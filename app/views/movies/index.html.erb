<% if !user_signed_in? %>
  <div class="jumbotron">
    <h1>  Your Favorite Movies Revied  </h1>
    <p>
      The Winter Soldier required innovative digital face-mapping techniques to create the effect of the character Falcon flying, ...
      The Winter Soldier required innovative digital face-mapping techniques to create the effect of the character Falcon flying, ...
    </p>
    <p>
      <%= link_to "Sign Up To Write a Review", new_user_registration_path, class:"btn btn-primary btn-lg" %>
    </p>
  </div>
<% end %>

<div class="row">
   <% @movies.each do |movie| %>
 <div class="col-sm-6 col-md-3">
     <div class="thumbnail">
       <%= link_to (image_tag movie.image.url(:medium), class: 'image'), movie %>
     </div>
 </div>
 <% end %>
</div>